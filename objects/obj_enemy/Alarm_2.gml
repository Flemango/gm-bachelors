/// @description delay of aiming
/*var target=obj_char;

if (image_xscale!=sign(target.x-x))
{
	alarm[0]=-1;
	state_set(states.prepare);
	
	if (target.x-x>0)
		image_xscale=1;
	else
		image_xscale=-1;
}*/

